--Run interpreter.py using any version of python 3.
--Default test case is test.txt file you can change it or edit interpreter.py to target it for any other file name
--You can see the syntax of our toy program in test.txt
--till now only if-else condition, while loop, assignment statement & binary operations has been added I will add more as we progress.
--There may be a few bugs in program but I hope to debug it soon enough. 